
export class Materia{
    
    nombre: string;
    horas: number;
    plan: string
    constructor(nombre: string, horas: number,plan: string){
        this.nombre=nombre;
        this.horas=horas;
        this.plan=plan;
    }
}